# Лабораторна робота №21  
## Тема: OCP — гнучкі алгоритми розрахунку (Factory Method, Strategy)

## Мета роботи
Метою лабораторної роботи є застосування принципу відкритості/закритості (Open/Closed Principle, OCP) для створення гнучкої та розширюваної системи розрахунку вартості доставки з використанням патернів Strategy та Factory Method.

---

## Теоретичні відомості
Принцип відкритості/закритості (OCP) є одним із принципів SOLID і стверджує, що програмні сутності повинні бути відкритими для розширення, але закритими для модифікації. Це означає, що нова функціональність повинна додаватися без зміни вже існуючого коду.

Патерн Strategy дозволяє інкапсулювати різні алгоритми та підмінювати їх під час виконання програми. Патерн Factory Method відповідає за створення обʼєктів, ізолюючи логіку створення від клієнтського коду.

---

## Опис реалізації

### Інтерфейс стратегії
- `IShippingStrategy` — визначає контракт для алгоритмів розрахунку вартості доставки.

```csharp
public interface IShippingStrategy
{
    decimal CalculateCost(decimal distance, decimal weight);
}
